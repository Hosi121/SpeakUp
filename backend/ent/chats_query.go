// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Hosi121/SpeakUp/ent/chats"
	"github.com/Hosi121/SpeakUp/ent/friends"
	"github.com/Hosi121/SpeakUp/ent/predicate"
)

// CHATSQuery is the builder for querying CHATS entities.
type CHATSQuery struct {
	config
	ctx        *QueryContext
	order      []chats.OrderOption
	inters     []Interceptor
	predicates []predicate.CHATS
	withHad    *FRIENDSQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CHATSQuery builder.
func (cq *CHATSQuery) Where(ps ...predicate.CHATS) *CHATSQuery {
	cq.predicates = append(cq.predicates, ps...)
	return cq
}

// Limit the number of records to be returned by this query.
func (cq *CHATSQuery) Limit(limit int) *CHATSQuery {
	cq.ctx.Limit = &limit
	return cq
}

// Offset to start from.
func (cq *CHATSQuery) Offset(offset int) *CHATSQuery {
	cq.ctx.Offset = &offset
	return cq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cq *CHATSQuery) Unique(unique bool) *CHATSQuery {
	cq.ctx.Unique = &unique
	return cq
}

// Order specifies how the records should be ordered.
func (cq *CHATSQuery) Order(o ...chats.OrderOption) *CHATSQuery {
	cq.order = append(cq.order, o...)
	return cq
}

// QueryHad chains the current query on the "had" edge.
func (cq *CHATSQuery) QueryHad() *FRIENDSQuery {
	query := (&FRIENDSClient{config: cq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(chats.Table, chats.FieldID, selector),
			sqlgraph.To(friends.Table, friends.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, chats.HadTable, chats.HadColumn),
		)
		fromU = sqlgraph.SetNeighbors(cq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CHATS entity from the query.
// Returns a *NotFoundError when no CHATS was found.
func (cq *CHATSQuery) First(ctx context.Context) (*CHATS, error) {
	nodes, err := cq.Limit(1).All(setContextOp(ctx, cq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{chats.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cq *CHATSQuery) FirstX(ctx context.Context) *CHATS {
	node, err := cq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CHATS ID from the query.
// Returns a *NotFoundError when no CHATS ID was found.
func (cq *CHATSQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cq.Limit(1).IDs(setContextOp(ctx, cq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{chats.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cq *CHATSQuery) FirstIDX(ctx context.Context) int {
	id, err := cq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CHATS entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CHATS entity is found.
// Returns a *NotFoundError when no CHATS entities are found.
func (cq *CHATSQuery) Only(ctx context.Context) (*CHATS, error) {
	nodes, err := cq.Limit(2).All(setContextOp(ctx, cq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{chats.Label}
	default:
		return nil, &NotSingularError{chats.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cq *CHATSQuery) OnlyX(ctx context.Context) *CHATS {
	node, err := cq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CHATS ID in the query.
// Returns a *NotSingularError when more than one CHATS ID is found.
// Returns a *NotFoundError when no entities are found.
func (cq *CHATSQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cq.Limit(2).IDs(setContextOp(ctx, cq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{chats.Label}
	default:
		err = &NotSingularError{chats.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cq *CHATSQuery) OnlyIDX(ctx context.Context) int {
	id, err := cq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CHATSs.
func (cq *CHATSQuery) All(ctx context.Context) ([]*CHATS, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryAll)
	if err := cq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CHATS, *CHATSQuery]()
	return withInterceptors[[]*CHATS](ctx, cq, qr, cq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cq *CHATSQuery) AllX(ctx context.Context) []*CHATS {
	nodes, err := cq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CHATS IDs.
func (cq *CHATSQuery) IDs(ctx context.Context) (ids []int, err error) {
	if cq.ctx.Unique == nil && cq.path != nil {
		cq.Unique(true)
	}
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryIDs)
	if err = cq.Select(chats.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cq *CHATSQuery) IDsX(ctx context.Context) []int {
	ids, err := cq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cq *CHATSQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryCount)
	if err := cq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cq, querierCount[*CHATSQuery](), cq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cq *CHATSQuery) CountX(ctx context.Context) int {
	count, err := cq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cq *CHATSQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryExist)
	switch _, err := cq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cq *CHATSQuery) ExistX(ctx context.Context) bool {
	exist, err := cq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CHATSQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cq *CHATSQuery) Clone() *CHATSQuery {
	if cq == nil {
		return nil
	}
	return &CHATSQuery{
		config:     cq.config,
		ctx:        cq.ctx.Clone(),
		order:      append([]chats.OrderOption{}, cq.order...),
		inters:     append([]Interceptor{}, cq.inters...),
		predicates: append([]predicate.CHATS{}, cq.predicates...),
		withHad:    cq.withHad.Clone(),
		// clone intermediate query.
		sql:  cq.sql.Clone(),
		path: cq.path,
	}
}

// WithHad tells the query-builder to eager-load the nodes that are connected to
// the "had" edge. The optional arguments are used to configure the query builder of the edge.
func (cq *CHATSQuery) WithHad(opts ...func(*FRIENDSQuery)) *CHATSQuery {
	query := (&FRIENDSClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cq.withHad = query
	return cq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FriendID int `json:"friend_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CHATS.Query().
//		GroupBy(chats.FieldFriendID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cq *CHATSQuery) GroupBy(field string, fields ...string) *CHATSGroupBy {
	cq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CHATSGroupBy{build: cq}
	grbuild.flds = &cq.ctx.Fields
	grbuild.label = chats.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FriendID int `json:"friend_id,omitempty"`
//	}
//
//	client.CHATS.Query().
//		Select(chats.FieldFriendID).
//		Scan(ctx, &v)
func (cq *CHATSQuery) Select(fields ...string) *CHATSSelect {
	cq.ctx.Fields = append(cq.ctx.Fields, fields...)
	sbuild := &CHATSSelect{CHATSQuery: cq}
	sbuild.label = chats.Label
	sbuild.flds, sbuild.scan = &cq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CHATSSelect configured with the given aggregations.
func (cq *CHATSQuery) Aggregate(fns ...AggregateFunc) *CHATSSelect {
	return cq.Select().Aggregate(fns...)
}

func (cq *CHATSQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cq); err != nil {
				return err
			}
		}
	}
	for _, f := range cq.ctx.Fields {
		if !chats.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cq.path != nil {
		prev, err := cq.path(ctx)
		if err != nil {
			return err
		}
		cq.sql = prev
	}
	return nil
}

func (cq *CHATSQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CHATS, error) {
	var (
		nodes       = []*CHATS{}
		withFKs     = cq.withFKs
		_spec       = cq.querySpec()
		loadedTypes = [1]bool{
			cq.withHad != nil,
		}
	)
	if cq.withHad != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, chats.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CHATS).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CHATS{config: cq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cq.withHad; query != nil {
		if err := cq.loadHad(ctx, query, nodes, nil,
			func(n *CHATS, e *FRIENDS) { n.Edges.Had = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cq *CHATSQuery) loadHad(ctx context.Context, query *FRIENDSQuery, nodes []*CHATS, init func(*CHATS), assign func(*CHATS, *FRIENDS)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CHATS)
	for i := range nodes {
		if nodes[i].friends_has == nil {
			continue
		}
		fk := *nodes[i].friends_has
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(friends.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "friends_has" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (cq *CHATSQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cq.querySpec()
	_spec.Node.Columns = cq.ctx.Fields
	if len(cq.ctx.Fields) > 0 {
		_spec.Unique = cq.ctx.Unique != nil && *cq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cq.driver, _spec)
}

func (cq *CHATSQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(chats.Table, chats.Columns, sqlgraph.NewFieldSpec(chats.FieldID, field.TypeInt))
	_spec.From = cq.sql
	if unique := cq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cq.path != nil {
		_spec.Unique = true
	}
	if fields := cq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, chats.FieldID)
		for i := range fields {
			if fields[i] != chats.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cq *CHATSQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cq.driver.Dialect())
	t1 := builder.Table(chats.Table)
	columns := cq.ctx.Fields
	if len(columns) == 0 {
		columns = chats.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cq.sql != nil {
		selector = cq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cq.ctx.Unique != nil && *cq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cq.predicates {
		p(selector)
	}
	for _, p := range cq.order {
		p(selector)
	}
	if offset := cq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CHATSGroupBy is the group-by builder for CHATS entities.
type CHATSGroupBy struct {
	selector
	build *CHATSQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cgb *CHATSGroupBy) Aggregate(fns ...AggregateFunc) *CHATSGroupBy {
	cgb.fns = append(cgb.fns, fns...)
	return cgb
}

// Scan applies the selector query and scans the result into the given value.
func (cgb *CHATSGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cgb.build.ctx, ent.OpQueryGroupBy)
	if err := cgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CHATSQuery, *CHATSGroupBy](ctx, cgb.build, cgb, cgb.build.inters, v)
}

func (cgb *CHATSGroupBy) sqlScan(ctx context.Context, root *CHATSQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cgb.fns))
	for _, fn := range cgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cgb.flds)+len(cgb.fns))
		for _, f := range *cgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CHATSSelect is the builder for selecting fields of CHATS entities.
type CHATSSelect struct {
	*CHATSQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cs *CHATSSelect) Aggregate(fns ...AggregateFunc) *CHATSSelect {
	cs.fns = append(cs.fns, fns...)
	return cs
}

// Scan applies the selector query and scans the result into the given value.
func (cs *CHATSSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cs.ctx, ent.OpQuerySelect)
	if err := cs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CHATSQuery, *CHATSSelect](ctx, cs.CHATSQuery, cs, cs.inters, v)
}

func (cs *CHATSSelect) sqlScan(ctx context.Context, root *CHATSQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cs.fns))
	for _, fn := range cs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
