// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Hosi121/SpeakUp/ent/event_records"
	"github.com/Hosi121/SpeakUp/ent/events"
	"github.com/Hosi121/SpeakUp/ent/predicate"
	"github.com/Hosi121/SpeakUp/ent/sessions"
	"github.com/Hosi121/SpeakUp/ent/users"
)

// EVENTRECORDSQuery is the builder for querying EVENT_RECORDS entities.
type EVENTRECORDSQuery struct {
	config
	ctx              *QueryContext
	order            []event_records.OrderOption
	inters           []Interceptor
	predicates       []predicate.EVENT_RECORDS
	withMade         *USERSQuery
	withParticipates *EVENTSQuery
	withHas          *SESSIONSQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EVENTRECORDSQuery builder.
func (eq *EVENTRECORDSQuery) Where(ps ...predicate.EVENT_RECORDS) *EVENTRECORDSQuery {
	eq.predicates = append(eq.predicates, ps...)
	return eq
}

// Limit the number of records to be returned by this query.
func (eq *EVENTRECORDSQuery) Limit(limit int) *EVENTRECORDSQuery {
	eq.ctx.Limit = &limit
	return eq
}

// Offset to start from.
func (eq *EVENTRECORDSQuery) Offset(offset int) *EVENTRECORDSQuery {
	eq.ctx.Offset = &offset
	return eq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eq *EVENTRECORDSQuery) Unique(unique bool) *EVENTRECORDSQuery {
	eq.ctx.Unique = &unique
	return eq
}

// Order specifies how the records should be ordered.
func (eq *EVENTRECORDSQuery) Order(o ...event_records.OrderOption) *EVENTRECORDSQuery {
	eq.order = append(eq.order, o...)
	return eq
}

// QueryMade chains the current query on the "made" edge.
func (eq *EVENTRECORDSQuery) QueryMade() *USERSQuery {
	query := (&USERSClient{config: eq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(event_records.Table, event_records.FieldID, selector),
			sqlgraph.To(users.Table, users.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, event_records.MadeTable, event_records.MadeColumn),
		)
		fromU = sqlgraph.SetNeighbors(eq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParticipates chains the current query on the "participates" edge.
func (eq *EVENTRECORDSQuery) QueryParticipates() *EVENTSQuery {
	query := (&EVENTSClient{config: eq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(event_records.Table, event_records.FieldID, selector),
			sqlgraph.To(events.Table, events.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, event_records.ParticipatesTable, event_records.ParticipatesColumn),
		)
		fromU = sqlgraph.SetNeighbors(eq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryHas chains the current query on the "has" edge.
func (eq *EVENTRECORDSQuery) QueryHas() *SESSIONSQuery {
	query := (&SESSIONSClient{config: eq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(event_records.Table, event_records.FieldID, selector),
			sqlgraph.To(sessions.Table, sessions.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, event_records.HasTable, event_records.HasColumn),
		)
		fromU = sqlgraph.SetNeighbors(eq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EVENT_RECORDS entity from the query.
// Returns a *NotFoundError when no EVENT_RECORDS was found.
func (eq *EVENTRECORDSQuery) First(ctx context.Context) (*EVENT_RECORDS, error) {
	nodes, err := eq.Limit(1).All(setContextOp(ctx, eq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{event_records.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eq *EVENTRECORDSQuery) FirstX(ctx context.Context) *EVENT_RECORDS {
	node, err := eq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EVENT_RECORDS ID from the query.
// Returns a *NotFoundError when no EVENT_RECORDS ID was found.
func (eq *EVENTRECORDSQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eq.Limit(1).IDs(setContextOp(ctx, eq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{event_records.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eq *EVENTRECORDSQuery) FirstIDX(ctx context.Context) int {
	id, err := eq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EVENT_RECORDS entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EVENT_RECORDS entity is found.
// Returns a *NotFoundError when no EVENT_RECORDS entities are found.
func (eq *EVENTRECORDSQuery) Only(ctx context.Context) (*EVENT_RECORDS, error) {
	nodes, err := eq.Limit(2).All(setContextOp(ctx, eq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{event_records.Label}
	default:
		return nil, &NotSingularError{event_records.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eq *EVENTRECORDSQuery) OnlyX(ctx context.Context) *EVENT_RECORDS {
	node, err := eq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EVENT_RECORDS ID in the query.
// Returns a *NotSingularError when more than one EVENT_RECORDS ID is found.
// Returns a *NotFoundError when no entities are found.
func (eq *EVENTRECORDSQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eq.Limit(2).IDs(setContextOp(ctx, eq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{event_records.Label}
	default:
		err = &NotSingularError{event_records.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eq *EVENTRECORDSQuery) OnlyIDX(ctx context.Context) int {
	id, err := eq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EVENT_RECORDSs.
func (eq *EVENTRECORDSQuery) All(ctx context.Context) ([]*EVENT_RECORDS, error) {
	ctx = setContextOp(ctx, eq.ctx, ent.OpQueryAll)
	if err := eq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EVENT_RECORDS, *EVENTRECORDSQuery]()
	return withInterceptors[[]*EVENT_RECORDS](ctx, eq, qr, eq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eq *EVENTRECORDSQuery) AllX(ctx context.Context) []*EVENT_RECORDS {
	nodes, err := eq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EVENT_RECORDS IDs.
func (eq *EVENTRECORDSQuery) IDs(ctx context.Context) (ids []int, err error) {
	if eq.ctx.Unique == nil && eq.path != nil {
		eq.Unique(true)
	}
	ctx = setContextOp(ctx, eq.ctx, ent.OpQueryIDs)
	if err = eq.Select(event_records.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eq *EVENTRECORDSQuery) IDsX(ctx context.Context) []int {
	ids, err := eq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eq *EVENTRECORDSQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eq.ctx, ent.OpQueryCount)
	if err := eq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eq, querierCount[*EVENTRECORDSQuery](), eq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eq *EVENTRECORDSQuery) CountX(ctx context.Context) int {
	count, err := eq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eq *EVENTRECORDSQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eq.ctx, ent.OpQueryExist)
	switch _, err := eq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eq *EVENTRECORDSQuery) ExistX(ctx context.Context) bool {
	exist, err := eq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EVENTRECORDSQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eq *EVENTRECORDSQuery) Clone() *EVENTRECORDSQuery {
	if eq == nil {
		return nil
	}
	return &EVENTRECORDSQuery{
		config:           eq.config,
		ctx:              eq.ctx.Clone(),
		order:            append([]event_records.OrderOption{}, eq.order...),
		inters:           append([]Interceptor{}, eq.inters...),
		predicates:       append([]predicate.EVENT_RECORDS{}, eq.predicates...),
		withMade:         eq.withMade.Clone(),
		withParticipates: eq.withParticipates.Clone(),
		withHas:          eq.withHas.Clone(),
		// clone intermediate query.
		sql:  eq.sql.Clone(),
		path: eq.path,
	}
}

// WithMade tells the query-builder to eager-load the nodes that are connected to
// the "made" edge. The optional arguments are used to configure the query builder of the edge.
func (eq *EVENTRECORDSQuery) WithMade(opts ...func(*USERSQuery)) *EVENTRECORDSQuery {
	query := (&USERSClient{config: eq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	eq.withMade = query
	return eq
}

// WithParticipates tells the query-builder to eager-load the nodes that are connected to
// the "participates" edge. The optional arguments are used to configure the query builder of the edge.
func (eq *EVENTRECORDSQuery) WithParticipates(opts ...func(*EVENTSQuery)) *EVENTRECORDSQuery {
	query := (&EVENTSClient{config: eq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	eq.withParticipates = query
	return eq
}

// WithHas tells the query-builder to eager-load the nodes that are connected to
// the "has" edge. The optional arguments are used to configure the query builder of the edge.
func (eq *EVENTRECORDSQuery) WithHas(opts ...func(*SESSIONSQuery)) *EVENTRECORDSQuery {
	query := (&SESSIONSClient{config: eq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	eq.withHas = query
	return eq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EVENTRECORDS.Query().
//		GroupBy(event_records.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (eq *EVENTRECORDSQuery) GroupBy(field string, fields ...string) *EVENTRECORDSGroupBy {
	eq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EVENTRECORDSGroupBy{build: eq}
	grbuild.flds = &eq.ctx.Fields
	grbuild.label = event_records.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//	}
//
//	client.EVENTRECORDS.Query().
//		Select(event_records.FieldUserID).
//		Scan(ctx, &v)
func (eq *EVENTRECORDSQuery) Select(fields ...string) *EVENTRECORDSSelect {
	eq.ctx.Fields = append(eq.ctx.Fields, fields...)
	sbuild := &EVENTRECORDSSelect{EVENTRECORDSQuery: eq}
	sbuild.label = event_records.Label
	sbuild.flds, sbuild.scan = &eq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EVENTRECORDSSelect configured with the given aggregations.
func (eq *EVENTRECORDSQuery) Aggregate(fns ...AggregateFunc) *EVENTRECORDSSelect {
	return eq.Select().Aggregate(fns...)
}

func (eq *EVENTRECORDSQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eq); err != nil {
				return err
			}
		}
	}
	for _, f := range eq.ctx.Fields {
		if !event_records.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eq.path != nil {
		prev, err := eq.path(ctx)
		if err != nil {
			return err
		}
		eq.sql = prev
	}
	return nil
}

func (eq *EVENTRECORDSQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EVENT_RECORDS, error) {
	var (
		nodes       = []*EVENT_RECORDS{}
		withFKs     = eq.withFKs
		_spec       = eq.querySpec()
		loadedTypes = [3]bool{
			eq.withMade != nil,
			eq.withParticipates != nil,
			eq.withHas != nil,
		}
	)
	if eq.withMade != nil || eq.withParticipates != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, event_records.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EVENT_RECORDS).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EVENT_RECORDS{config: eq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := eq.withMade; query != nil {
		if err := eq.loadMade(ctx, query, nodes, nil,
			func(n *EVENT_RECORDS, e *USERS) { n.Edges.Made = e }); err != nil {
			return nil, err
		}
	}
	if query := eq.withParticipates; query != nil {
		if err := eq.loadParticipates(ctx, query, nodes, nil,
			func(n *EVENT_RECORDS, e *EVENTS) { n.Edges.Participates = e }); err != nil {
			return nil, err
		}
	}
	if query := eq.withHas; query != nil {
		if err := eq.loadHas(ctx, query, nodes,
			func(n *EVENT_RECORDS) { n.Edges.Has = []*SESSIONS{} },
			func(n *EVENT_RECORDS, e *SESSIONS) { n.Edges.Has = append(n.Edges.Has, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (eq *EVENTRECORDSQuery) loadMade(ctx context.Context, query *USERSQuery, nodes []*EVENT_RECORDS, init func(*EVENT_RECORDS), assign func(*EVENT_RECORDS, *USERS)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*EVENT_RECORDS)
	for i := range nodes {
		if nodes[i].users_makes == nil {
			continue
		}
		fk := *nodes[i].users_makes
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(users.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "users_makes" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (eq *EVENTRECORDSQuery) loadParticipates(ctx context.Context, query *EVENTSQuery, nodes []*EVENT_RECORDS, init func(*EVENT_RECORDS), assign func(*EVENT_RECORDS, *EVENTS)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*EVENT_RECORDS)
	for i := range nodes {
		if nodes[i].events_participated == nil {
			continue
		}
		fk := *nodes[i].events_participated
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(events.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "events_participated" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (eq *EVENTRECORDSQuery) loadHas(ctx context.Context, query *SESSIONSQuery, nodes []*EVENT_RECORDS, init func(*EVENT_RECORDS), assign func(*EVENT_RECORDS, *SESSIONS)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*EVENT_RECORDS)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.SESSIONS(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(event_records.HasColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.event_records_has
		if fk == nil {
			return fmt.Errorf(`foreign-key "event_records_has" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "event_records_has" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (eq *EVENTRECORDSQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eq.querySpec()
	_spec.Node.Columns = eq.ctx.Fields
	if len(eq.ctx.Fields) > 0 {
		_spec.Unique = eq.ctx.Unique != nil && *eq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eq.driver, _spec)
}

func (eq *EVENTRECORDSQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(event_records.Table, event_records.Columns, sqlgraph.NewFieldSpec(event_records.FieldID, field.TypeInt))
	_spec.From = eq.sql
	if unique := eq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if eq.path != nil {
		_spec.Unique = true
	}
	if fields := eq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, event_records.FieldID)
		for i := range fields {
			if fields[i] != event_records.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eq *EVENTRECORDSQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eq.driver.Dialect())
	t1 := builder.Table(event_records.Table)
	columns := eq.ctx.Fields
	if len(columns) == 0 {
		columns = event_records.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eq.sql != nil {
		selector = eq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eq.ctx.Unique != nil && *eq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range eq.predicates {
		p(selector)
	}
	for _, p := range eq.order {
		p(selector)
	}
	if offset := eq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EVENTRECORDSGroupBy is the group-by builder for EVENT_RECORDS entities.
type EVENTRECORDSGroupBy struct {
	selector
	build *EVENTRECORDSQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (egb *EVENTRECORDSGroupBy) Aggregate(fns ...AggregateFunc) *EVENTRECORDSGroupBy {
	egb.fns = append(egb.fns, fns...)
	return egb
}

// Scan applies the selector query and scans the result into the given value.
func (egb *EVENTRECORDSGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, egb.build.ctx, ent.OpQueryGroupBy)
	if err := egb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EVENTRECORDSQuery, *EVENTRECORDSGroupBy](ctx, egb.build, egb, egb.build.inters, v)
}

func (egb *EVENTRECORDSGroupBy) sqlScan(ctx context.Context, root *EVENTRECORDSQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(egb.fns))
	for _, fn := range egb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*egb.flds)+len(egb.fns))
		for _, f := range *egb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*egb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := egb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EVENTRECORDSSelect is the builder for selecting fields of EVENTRECORDS entities.
type EVENTRECORDSSelect struct {
	*EVENTRECORDSQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (es *EVENTRECORDSSelect) Aggregate(fns ...AggregateFunc) *EVENTRECORDSSelect {
	es.fns = append(es.fns, fns...)
	return es
}

// Scan applies the selector query and scans the result into the given value.
func (es *EVENTRECORDSSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, es.ctx, ent.OpQuerySelect)
	if err := es.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EVENTRECORDSQuery, *EVENTRECORDSSelect](ctx, es.EVENTRECORDSQuery, es, es.inters, v)
}

func (es *EVENTRECORDSSelect) sqlScan(ctx context.Context, root *EVENTRECORDSQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(es.fns))
	for _, fn := range es.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*es.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := es.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
