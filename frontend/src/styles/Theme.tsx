import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    background: {
      default: "#F2D66F",
    },
    primary: {
      main: "#F71F81",
    },
    secondary: {
      main: "#FFFFFF",
    },
  },
});

export default Theme;
